// Default time rule configuration

const DEFAULT_TIME_RULES = {
  work_start_time: '09:00',
  work_end_time: '17:00',
  start_grace_minutes: 15,
  end_grace_minutes: 15,
  standard_work_hours: 8,
  max_shift_hours: 24,
};

module.exports = { DEFAULT_TIME_RULES };
